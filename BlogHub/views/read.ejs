<%- include("header.ejs") %>

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Blogs</title>
    <link rel="stylesheet" href="styles.css">
</head>
<body>
    <h1>Your Blogs</h1>
    <% if (blogs.length > 0) { %>
        <ul>
            <% blogs.forEach(blog => { %>
                <li>
                    <h2><%= blog.title %></h2>
                    <p><%= blog.content %></p>
                </li>
            <% }) %>
        </ul>
    <% } else { %>
        <p>No blogs available. You can create a new blog from the homepage.</p>
    <% } %>
    <a href="/">Back to Home</a>
</body>
</html>
<%- include("footer.ejs") %>